# Improvement Tracker

Last reviewed: 2026-08-05. IMP-004 is the only open requested improvement.

Use this document to log requested improvements for the inventory app as they come in.

## Status

- Open: requested and not yet implemented
- In progress: actively being worked
- Done: implemented and verified
- Deferred: intentionally postponed

## Requested Improvements

| ID | Date Logged | Area | Request | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| IMP-001 | 2026-08-03 | Find Item page | Change initial page load so it only loads search data for the name and barcode columns without rendering item cards. Require the user to click Search to run an API-backed search across the full dataset and render matching cards. Add a Load All Items button next to the search bar. Keep scan behavior unchanged. | Done | Implemented optional `/items/?q=`, explicit Search/Enter, Load All Items, direct scan-result rendering, and an API-backed Add Barcode picker. Follow-up on 2026-08-05 removed the native name/barcode datalist and its initial request because the browser popup overwhelmed the screen; Find Item now stays visually empty until Search, Load All, or Scan. The `/items/search-index` endpoint remains for compatibility. |
| IMP-002 | 2026-08-03 | User model, Add User page, CSV import, work orders | Add First Name and Last Name fields to user creation. During CSV import, match users by first + last name. If no matching first + last name exists, default the association to `unassigned` as a fallback for misspellings or mismatches. Keep usernames visible only for account creation, the Users page, and login. Anywhere tied to a work order should display first + last names instead of usernames. | Done | Added nullable legacy-safe user name columns plus required names for new accounts and a Users-page Edit Name remediation flow. CSV routing now matches unambiguous active-supervisor full names; misses, incomplete names, archived/non-supervisor matches, and duplicates stay unassigned. Operational API and UI contracts use full names without login usernames; usernames remain on login/account-management views. Verified by 299 backend tests, all JavaScript syntax checks, Python compilation, Alembic upgrade to `f3b5d7a9c1e2`, OpenAPI contract checks, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-003 | 2026-08-03 | Scan / Stock page | Move Enter Work Order Number into its own smaller card positioned above the work order cards. Show Add Item Manually only after a work order is selected. Change the manual work order number entry behavior so it acts more like a search/filter control than a standard input field. | Done | Added a compact Supervisor+ search card above the scoped work-order cards. Typing now performs a debounced server-side number filter and only selecting a result starts the batch; stale responses are ignored. Add Item Manually remains hidden until a work order is active. Verified by 299 backend tests, syntax checks for all 29 frontend JavaScript files, Python compilation, served-page/DOM integration, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-004 | 2026-08-03 | Mass Stage | Make New Mass Stage a collapsible card that is collapsed by default. Remove the redundant Unit # field under Communities. Change the workflow so the user first inputs/searches a work order number, where search only queries the work order number, and then loads the rest of the work order data after a search result is selected. Use mass staging primarily to group work orders by Location. If a saved Community from mass staging appears in a work order’s Location field, display that work order under the Communities cards. | Open | Very low priority. Request logged only; no implementation yet. |
| IMP-005 | 2026-08-03 | Work orders | Expand work-order statuses to `Created`, `Assigned`, `In-Progress`, `Completed`, `Review`, and `Closed`. Apply card background color coding by status with contrasting text colors: Created = Gray, Assigned = Red, In-Progress = Yellow, Completed = Blue, Review = Green. `Closed` should represent an archived work order, and work orders may only be closed from the Admin Review page. | Done | Added the expanded lifecycle and requested card colors. The original completion behavior was later superseded: IMP-014 made general lifecycle controls Supervisor+ only, IMP-019 added back only the narrow assigned Technician start action in Scan/Stock, and IMP-015 allows Admin+ archive from any live status while retaining Admin Review's receipt-aware Close flow. Created/Assigned remains technician-derived, first material/labor activity derives In-Progress, and Closed remains `archived_at`. |
| IMP-006 | 2026-08-03 | Work orders | Allow supervisors to assign multiple technicians to a single work order. Track labor hours beneath item selection on the work order, with labor billed at $62.50/hour in 30-minute increments and always rounded up to the next 30 minutes. | Done | Added normalized plural `work_order_technicians` assignments with existing singular values backfilled and retained as a compatibility mirror. Every assigned worker receives scoped access. IMP-023 later replaced the complete checkbox catalog with a searchable multi-select editor, and IMP-027 later made active Supervisors eligible as technician-role workers. Actual-minute labor CRUD sits beneath material selection; IMP-014 subsequently made all labor writes Supervisor+ while preserving labor attribution to any assigned worker. The combined duration rounds upward once to 30 minutes at $62.50/hour. |
| IMP-007 | 2026-08-03 | Admin Review page | Add a new Admin Review page that displays every work order currently in `Review` status as a card, using the work order number as the card title. Clicking a card renders a persistent embedded receipt with the same 41-character behavior as History, authoritative marked-up material lines, `[x] Labor Hours` plus its charge, and the combined total. The copied receipt must not include a work-order-number header. Selecting a different card overwrites the box. | Done | Added the Admin/Owner queue, shared pure 41-character formatter, override-aware receipt builder, always-present billed-labor line without extra mark-up, missing-price Close block, confirmed Return to In-Progress, and confirmed Review-to-Closed archive. Queue actions preserve the current receipt for copying. Verified by 32 JavaScript syntax checks, pure receipt assertions, 53 focused backend billing/role tests, Python compilation, served SPA/DOM plus changed-resource checks, and `git diff --check`. Full backend run: 322 passed and one unrelated isolation assertion failed because the configured test database already contained one persisted `work_order_items` row before the test transaction; interactive browser QA was unavailable. |
| IMP-008 | 2026-08-03 | Navigation / page routing | Keep navbar navigation SPA-style without reloading the document. Refresh current server data every time Work Orders, Find Item, or Mass Stage is navigated to. | Done | Navbar buttons switch in-memory views through `showPage`. Every Find Item activation clears rendered results and leaves the plain search view empty until Search, Load All, or Scan; Work Orders and Mass Stage freshly load their main lists plus item/user reference data on every page entry. Dynamic authenticated list requests bypass the browser HTTP cache. |
| IMP-009 | 2026-08-03 | Work orders | Add a way in Edit Details to change and roll back a work order's status from Completed and every earlier step. Add `On-Hold` as an editable status. | Done | Supervisor+ Edit details offers non-Review status management plus On-Hold; a held order can be explicitly resumed. Created/Assigned remains derived from technician assignment, On-Hold is stable during activity, and rollback clears `completed_at`. IMP-014 later made every general lifecycle control Supervisor+; IMP-019 adds back only the scoped Technician Assigned-to-In-Progress Scan/Stock action; IMP-024 makes this existing dropdown the explicit Supervisor start control too. On-Hold cards/filter use an orange treatment. |
| IMP-010 | 2026-08-03 | Work orders | Add a Notes section where users can freely type notes associated with a work order. | Done | Added nullable free-form `work_orders.notes` through migration `f6e8a0b2d4f5`. IMP-026 later changed its UI/API behavior from replace/clear to an append-only authored and timestamped text log while preserving the same column and legacy content. |
| IMP-011 | 2026-08-03 | Work orders / Scan & Stock | Let technicians click a button to set a Work Order In-Progress. If a user selects a Scan/Stock work order that is not In-Progress, show a pop-up offering to go set it. | Done | The original Work Orders-card action shipped before IMP-014 narrowed general status controls to Supervisor+. IMP-019 preserves the Technician start need through the separate scoped Scan/Stock action without restoring general Technician status editing: Assigned starts in place, while Created still confirms and opens Work Orders for assignment. IMP-024 removes the now-redundant standalone Supervisor button after making the existing Edit details status dropdown the equivalent explicit start path. On-Hold stays Supervisor-controlled and is excluded from Scan/Stock. |
| IMP-012 | 2026-08-04 | Work Orders search | Add joinable Work Orders filters for status, service type, assigned supervisor, and community so combinations such as In-Progress + one supervisor + one community return only the intersection. Derive Scholars, Centennial, Commons (including Cimarron/Cimmarron), Young Hall, and Academics from location text. | Done | Added server-side AND filters plus number search, caller-scoped dynamic service/supervisor options, stable community choices, multi-community membership for raw multi-location rows, and Academics as the no-known-term fallback. Any active filter searches the full scoped set; Clear filters restores the recent-10 browse. Verified by 409 backend tests, all 32 JavaScript syntax checks, OpenAPI/served-DOM contracts, and `git diff --check`. |
| IMP-013 | 2026-08-04 | Work Orders search / export | Move the operational CSV export into the Work Orders search card and export exactly the current filtered set while leaving For Client unchanged. Sort cards by most recent Scheduled Date and add an exact date filter. | Done | Export filtered CSV sends the active status, service, supervisor, community, scheduled-date, and number predicates to the uncapped operational export; For Client remains scope-only. Raw vendor dates are parsed safely (`M/D/YYYY`, two-digit year, optional time, or ISO), sorted descending with invalid/blank values last, and filterable by calendar day. Verified by 414 backend tests, all 32 JavaScript syntax checks, OpenAPI parameter contracts, served-DOM placement, and `git diff --check`. |
| IMP-014 | 2026-08-04 | Work Orders role UX | Minimize expanded Work Order controls by role: Admin+ may edit imported metadata; Supervisors edit only supervisor, technicians, status, and labor; Technicians edit notes and add items only. | Done | Added a service-layer least-privilege matrix: notes/add-material remain Technician+; operational routing/status/mode, labor, and logged-material corrections/removal require Supervisor+; Location, Service, Schedule Date, Output To, Vendor Contact, Symptom/Task, and other metadata require Admin+. The editor renders only those role-relevant controls. Verified by 415 backend tests, all 32 JavaScript syntax checks, focused role coverage, and `git diff --check`. |
| IMP-015 | 2026-08-04 | Work Orders archive | Allow Admin+ to archive a work order during any status. | Done | Removed the Review-only service restriction, added an authoritative Admin+ service gate, and exposed a confirmed Archive action on every expanded live Work Order card for Admin/Owner. Admin Review retains its receipt-aware Close action for Review rows. Verified by 427 backend tests, all 32 JavaScript syntax checks, and Python compilation. This supersedes IMP-005/IMP-007's earlier Review-only UI placement. |
| IMP-016 | 2026-08-04 | Work Orders CSV import | Do not restore archived work orders during import; report how many CSV rows are already closed and leave every closed row untouched. | Done | Import now skips archived matches before supervisor routing or metadata merge, returns a dedicated `closed` count included in `total`, and always shows that count in the completion message. Live matches retain fill-blanks-only metadata behavior; notes, assignments, lifecycle, materials, labor, billing, and existing metadata are untouched. Verified by the 428-test backend suite, including a closed-row preservation regression. |
| IMP-017 | 2026-08-04 | Work Orders CSV export | Name export files `[TIMESTAMP]_[filter1]-[filter2]`, using readable date/time. | Done | Server-generated attachment names use `MM-DD-YY_HH-MM_filters.csv` in UTC and every active operational filter value; the time separator is `-` because Windows forbids `:` in filenames. No-filter files use `_all.csv`, while For Client uses `client-<scope>`. Focused export tests cover all, multi-filter, and client filenames. |
| IMP-018 | 2026-08-04 | Work Orders supervisor pickup / CSV import | Let supervisors see unassigned work orders plus work routed to themselves so they can pick up work. If another supervisor wins a stale pickup, show `This Work Order was already assigned to [First] [Last]` and reload after the prompt closes. Re-import must assign a supervisor only while the work order remains unassigned and must preserve a manual reroute. | Done | Supervisor scope began as unassigned-or-self-routed. Edit Details sends `expected_supervisor_id`; the service locks the row, returns a named 409 on stale pickup, and still returns a successful transfer even when it moves out of caller scope. Import/reference merging locks the same row and fills `supervisor_id` only when NULL, including insert-race recovery. IMP-027 later expanded eligible routing targets to active Admins and Supervisors and lets worker-assigned Supervisors retain scope under other routing. The one-button conflict prompt reloads on dismissal, and import copy reports name matches rather than claiming every match was routed. |
| IMP-019 | 2026-08-05 | Scan & Stock / Work Orders | For Supervisor and Technician, accepting the confirmation on an Assigned Work Order must move it to In-Progress without leaving Scan/Stock. Let either role remove a mistaken scan. | Done | Added the narrow, scoped, Technician+ `POST /work-orders/{id}/start` transition and wired Assigned cards to start and select the batch in place. Every successful scan has Remove; Supervisor+ may void any transaction, while a Technician may void only their own work-order-linked dispense. Removal reverses inventory and the work-order material line. |
| IMP-020 | 2026-08-05 | Scan & Stock / User Requests | Permit a dispense when counted stock is insufficient, show a red `Please re-count stock` indicator, and give Admin+ a generic User Requests page for tracking and resolving count disparities. | Done | Scan/Stock now records the dispense and lets expected quantity become negative, returns the authoritative quantity/recount flag, and creates one open `inventory_recount` request linked to that transaction, item, work order, and requestor. Removing the scan resolves its request automatically. Admin/Owner can filter open/resolved requests, resolve them, or reopen them; the API also accepts an optional resolution note. The `user_requests` table and endpoints are generic enough for later request types. Verified by 445 backend tests, all 33 JavaScript syntax checks, Python compilation, OpenAPI (69 operations), Alembic `f8a0c2e4b6d8` at head, and `git diff --check`; interactive browser automation was unavailable because the browser runtime could not initialize. |
| IMP-021 | 2026-08-05 | Work Orders / User Requests / item pricing | Track an item used on a work order without a price. Let Admin/Owner add both price and product link from the same request, and remove it from the open queue automatically once complete. | Done | Every work-order material path uses `attach_dispense_line`, which now creates one open `missing_item_price` request per item with a NULL or non-positive price (`$0.00` included) and accumulates affected work-order numbers instead of duplicating cards, including multi-work-order Mass Stage allocations in one no-autoflush transaction. The open card exposes Price and Product Link and saves both through one item PATCH. Item updates auto-resolve the request only when the price is greater than zero and a nonblank link exists; work-order totals use the live item price immediately, and the durable request remains in resolved history. Migrations `f9b1d3e5a7c9` and `faa2c4e6b8d0` backfill existing live work-order items. Verified by 450 backend tests, 33 JavaScript syntax checks, Python compilation, served HTML/OpenAPI contracts, Alembic head, and `git diff --check`; interactive browser automation remained unavailable because its runtime could not initialize. |
| IMP-022 | 2026-08-05 | Work Orders / User Requests / stock count | Let Technicians and Supervisors add an item from the Work Orders page even when recorded stock is zero, and create the same recount request used by Scan/Stock. | Done | The scoped Add Item service now records a dispense-mode line despite insufficient stock, preserves the negative expected balance for exact reversal, and atomically creates a transaction-linked `inventory_recount` request with recorded-before, dispensed, shortage, work order, and requestor context. The Work Orders card reports `Please re-count stock` in red. Retroactive entries remain stock-neutral; later quantity edits remain strict. Supervisor removal restores stock, voids contributing transactions, and resolves their requests. Verified by 451 backend tests, all 33 JavaScript syntax checks, Python compilation, OpenAPI (69 operations), Alembic `faa2c4e6b8d0` at head, and `git diff --check`; per project direction, the owner performs in-browser interaction tests manually. |
| IMP-023 | 2026-08-05 | Work Orders technician assignment | Replace the Edit Details list of every Technician with a searchable dropdown at the top of the assignment box. Show assigned Technicians beneath it and continue allowing multiple assignments. | Done | Replaced the checkbox catalog with a local full-name search over the existing active-user reference list. Blank search shows no catalog; results exclude already selected users and are capped at eight. Selecting adds a removable row beneath the search, clearing every row sends an empty plural assignment set, Escape/outside focus closes results, and Save continues using the existing `assigned_to_ids` replacement API. No backend, endpoint, schema, or migration change. Verified by 451 backend tests, all 33 JavaScript syntax checks, Python compilation, a focused picker source-contract check, and `git diff --check`; browser click-through remains owner-performed. |
| IMP-024 | 2026-08-05 | Work Orders card layout / status | Put added materials in a labeled card like Notes and Labor. Replace the Edit details button with a collapsible Details card that is closed by default, and remove the redundant Set In-Progress button because status is managed through the existing dropdown. | Done | The read-only overview remains visible while a native nested Edit details card holds the existing role-scoped form and starts collapsed. Created/Assigned now includes In-Progress in that existing dropdown, preserving explicit Supervisor start capability before the standalone control and handler were removed. Save/Cancel re-fetch and collapse the editor. Logged rows, the empty state, Admin total, and add-material controls now share one Materials card. No backend/API/schema/migration change. Verified by 451 backend tests, all 33 JavaScript syntax checks, Python compilation, a focused nested-card/status/materials source contract, and `git diff --check`; browser click-through remains owner-performed. |
| IMP-025 | 2026-08-05 | Work Orders inner cards | Make Notes, Materials, and Labor collapsible cards. Materials and Labor must remain open when their data changes; Notes should close after Save. | Done | Converted all three regions to native nested `details` cards with shared summary/content styling and a collapsed default. The Work Order refresh helper accepts a section to reopen; material add/update/remove/billing and labor add/update/remove use it so refreshed data remains visible. Notes does not need a refresh and closes itself after a successful save. Roles, APIs, and persistence are unchanged. Verified by 451 backend tests, all 33 JavaScript syntax checks, Python compilation, a focused collapsible-card/state source contract, and `git diff --check`; browser click-through remains owner-performed. |
| IMP-026 | 2026-08-05 | Work Orders notes log | Keep every saved note as a viewable text log entry. Prefix each entry with `[TIME] [MMDDYY]` and the user who submitted it; clear the entry box and close Notes after Save. | Done | Reused the existing `work_orders.notes` text column without a migration. Under the already-locked Work Order update, `append_note_log` preserves prior/legacy text and adds `[h:mm AM/PM] [MMDDYY] [Full Name] note` using server time converted to `America/Chicago` and the authenticated user's `full_name`; null cannot erase the log. Notes renders the saved log separately above a blank entry textarea. A successful response replaces the visible log, clears the textarea, and closes the card; blank input stays open with validation. Verified by 452 backend tests, all 33 JavaScript syntax checks, Python compilation, a focused authored/timestamped append-log source contract, and `git diff --check`; browser click-through remains owner-performed. |
| IMP-027 | 2026-08-05 | Work Orders role assignment | Let every active Admin be selected as a work-order Supervisor. Let every active Supervisor assign themselves or another Supervisor as a technician-role worker. | Done | Centralized assignment eligibility in backend/frontend role helpers: Supervisor routing accepts active Admin or Supervisor accounts, while technician-role assignment accepts active Technician or Supervisor accounts; Owners remain globally authorized but are not selectable targets. Work Orders and Mass Stage use the same worker pool, CSV full-name routing recognizes Admins, labor accepts any assigned worker, and a Supervisor assigned as a worker retains list/detail scope even when another Admin/Supervisor owns routing. Verified by 457 backend tests, all 33 JavaScript syntax checks, Python compilation, and `git diff --check`; browser click-through remains owner-performed. |
