# Improvement Tracker

Last reviewed: 2026-08-04. IMP-004 is the only open requested improvement.

Use this document to log requested improvements for the inventory app as they come in.

## Status

- Open: requested and not yet implemented
- In progress: actively being worked
- Done: implemented and verified
- Deferred: intentionally postponed

## Requested Improvements

| ID | Date Logged | Area | Request | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| IMP-001 | 2026-08-03 | Find Item page | Change initial page load so it only loads search data for the name and barcode columns without rendering item cards. Require the user to click Search to run an API-backed search across the full dataset and render matching cards. Add a Load All Items button next to the search bar. Keep scan behavior unchanged. | Done | Implemented with `/items/search-index`, optional `/items/?q=`, explicit Search/Enter, Load All Items, direct scan-result rendering, and an API-backed Add Barcode picker. Verified by 285 backend tests, all JavaScript syntax checks, Python compilation, and `git diff --check`; interactive browser QA was unavailable in this session. |
| IMP-002 | 2026-08-03 | User model, Add User page, CSV import, work orders | Add First Name and Last Name fields to user creation. During CSV import, match users by first + last name. If no matching first + last name exists, default the association to `unassigned` as a fallback for misspellings or mismatches. Keep usernames visible only for account creation, the Users page, and login. Anywhere tied to a work order should display first + last names instead of usernames. | Done | Added nullable legacy-safe user name columns plus required names for new accounts and a Users-page Edit Name remediation flow. CSV routing now matches unambiguous active-supervisor full names; misses, incomplete names, archived/non-supervisor matches, and duplicates stay unassigned. Operational API and UI contracts use full names without login usernames; usernames remain on login/account-management views. Verified by 299 backend tests, all JavaScript syntax checks, Python compilation, Alembic upgrade to `f3b5d7a9c1e2`, OpenAPI contract checks, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-003 | 2026-08-03 | Scan / Stock page | Move Enter Work Order Number into its own smaller card positioned above the work order cards. Show Add Item Manually only after a work order is selected. Change the manual work order number entry behavior so it acts more like a search/filter control than a standard input field. | Done | Added a compact Supervisor+ search card above the scoped work-order cards. Typing now performs a debounced server-side number filter and only selecting a result starts the batch; stale responses are ignored. Add Item Manually remains hidden until a work order is active. Verified by 299 backend tests, syntax checks for all 29 frontend JavaScript files, Python compilation, served-page/DOM integration, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-004 | 2026-08-03 | Mass Stage | Make New Mass Stage a collapsible card that is collapsed by default. Remove the redundant Unit # field under Communities. Change the workflow so the user first inputs/searches a work order number, where search only queries the work order number, and then loads the rest of the work order data after a search result is selected. Use mass staging primarily to group work orders by Location. If a saved Community from mass staging appears in a work order’s Location field, display that work order under the Communities cards. | Open | Very low priority. Request logged only; no implementation yet. |
| IMP-005 | 2026-08-03 | Work orders | Expand work-order statuses to `Created`, `Assigned`, `In-Progress`, `Completed`, `Review`, and `Closed`. Apply card background color coding by status with contrasting text colors: Created = Gray, Assigned = Red, In-Progress = Yellow, Completed = Blue, Review = Green. `Closed` should represent an archived work order, and work orders may only be closed from the Admin Review page. | Done | Added five stored live states with corrected lifecycle derivation: every import starts Created, technician assignment derives Assigned, and first committed material or labor derives In-Progress without changing status on card selection. Technicians and supervisors can Mark completed; only Supervisor+ sees Send to Review and must confirm readiness in a pop-up. Closed maps to Review-only Admin+ archive, with no ordinary Work Orders close action. Cards use the requested colors. Verified by 307 backend tests, 29 JavaScript syntax checks, Python compilation, Alembic upgrade to `f5d7f9b1c3e4`, served-resource/DOM integration, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-006 | 2026-08-03 | Work orders | Allow supervisors to assign multiple technicians to a single work order. Track labor hours beneath item selection on the work order, with labor billed at $62.50/hour in 30-minute increments and always rounded up to the next 30 minutes. | Done | Added normalized plural technician assignments with existing singular values backfilled and retained as a compatibility mirror. Every assigned technician receives scoped access. Per-technician actual-minute labor CRUD sits beneath material selection; technicians are self-only, Supervisor+ can manage any assigned technician, and the combined duration rounds upward once to 30 minutes at $62.50/hour. Verified by 323 backend tests, 29 JavaScript syntax checks, Python compilation, Alembic upgrade to `f7a9b1c3d5e6`, OpenAPI/served-resource checks, and `git diff --check`; interactive browser QA was unavailable. |
| IMP-007 | 2026-08-03 | Admin Review page | Add a new Admin Review page that displays every work order currently in `Review` status as a card, using the work order number as the card title. Clicking a card renders a persistent embedded receipt with the same 41-character behavior as History, authoritative marked-up material lines, `[x] Labor Hours` plus its charge, and the combined total. The copied receipt must not include a work-order-number header. Selecting a different card overwrites the box. | Done | Added the Admin/Owner queue, shared pure 41-character formatter, override-aware receipt builder, always-present billed-labor line without extra mark-up, missing-price Close block, confirmed Return to In-Progress, and confirmed Review-to-Closed archive. Queue actions preserve the current receipt for copying. Verified by 32 JavaScript syntax checks, pure receipt assertions, 53 focused backend billing/role tests, Python compilation, served SPA/DOM plus changed-resource checks, and `git diff --check`. Full backend run: 322 passed and one unrelated isolation assertion failed because the configured test database already contained one persisted `work_order_items` row before the test transaction; interactive browser QA was unavailable. |
| IMP-008 | 2026-08-03 | Navigation / page routing | Keep navbar navigation SPA-style without reloading the document. Refresh current server data every time Work Orders, Find Item, or Mass Stage is navigated to. | Done | Navbar buttons switch in-memory views through `showPage`. Every Find Item activation clears rendered results and freshly loads its lightweight search index; Work Orders and Mass Stage freshly load their main lists plus item/user reference data on every page entry. Dynamic authenticated list requests bypass the browser HTTP cache. |
| IMP-009 | 2026-08-03 | Work orders | Add a way in Edit Details to change and roll back a work order's status from Completed and every earlier step. Add `On-Hold` as an editable status. | Done | Supervisor+ Edit details now offers current/earlier non-Review steps plus On-Hold; a held order can be explicitly resumed. Created/Assigned remains derived from technician assignment, On-Hold is stable during activity, and rollback clears `completed_at`. Rollback/On-Hold/Review are backend-gated to Supervisor+ while technicians retain Set In-Progress and Mark Completed. On-Hold cards/filter use an orange treatment. Verified with IMP-010/011 in the current 317-test suite. |
| IMP-010 | 2026-08-03 | Work orders | Add a Notes section where users can freely type notes associated with a work order. | Done | Added nullable free-form `work_orders.notes` through migration `f6e8a0b2d4f5`. Every expanded card exposes a Notes textarea and Save notes action to any in-scope user; whitespace is trimmed and blank saves clear the field. Detail responses include notes without adding them to list cards. Verified with IMP-009 by 317 backend tests, 29 JavaScript syntax checks, Python compilation, Alembic upgrade, and served-resource/DOM checks; interactive browser QA was unavailable. |
| IMP-011 | 2026-08-03 | Work orders / Scan & Stock | Let technicians click a button to set a Work Order In-Progress. If a user selects a Scan/Stock work order that is not In-Progress, show a pop-up offering to go set it. | Done | Created/Assigned Work Order cards expose Set In-Progress to any in-scope user and the backend permits technicians to make that transition without allowing them to reopen Completed/On-Hold/Review. Technicians can then Mark Completed; a Supervisor reviews the completed work and confirms Send to Review, placing it into final Admin Review. Scan/Stock starts only from In-Progress; selecting Created/Assigned shows a confirmation, then either navigates to and expands that Work Order or stays at the gate. On-Hold stays Supervisor-controlled and is excluded from Scan/Stock. Verified in the current 317-test suite plus frontend syntax and served DOM/action checks. |
| IMP-012 | 2026-08-04 | Work Orders search | Add joinable Work Orders filters for status, service type, assigned supervisor, and community so combinations such as In-Progress + one supervisor + one community return only the intersection. Derive Scholars, Centennial, Commons (including Cimarron/Cimmarron), Young Hall, and Academics from location text. | Done | Added server-side AND filters plus number search, caller-scoped dynamic service/supervisor options, stable community choices, multi-community membership for raw multi-location rows, and Academics as the no-known-term fallback. Any active filter searches the full scoped set; Clear filters restores the recent-10 browse. Verified by 409 backend tests, all 32 JavaScript syntax checks, OpenAPI/served-DOM contracts, and `git diff --check`. |
| IMP-013 | 2026-08-04 | Work Orders search / export | Move the operational CSV export into the Work Orders search card and export exactly the current filtered set while leaving For Client unchanged. Sort cards by most recent Scheduled Date and add an exact date filter. | Done | Export filtered CSV sends the active status, service, supervisor, community, scheduled-date, and number predicates to the uncapped operational export; For Client remains scope-only. Raw vendor dates are parsed safely (`M/D/YYYY`, two-digit year, optional time, or ISO), sorted descending with invalid/blank values last, and filterable by calendar day. Verified by 414 backend tests, all 32 JavaScript syntax checks, OpenAPI parameter contracts, served-DOM placement, and `git diff --check`. |
| IMP-014 | 2026-08-04 | Work Orders role UX | Minimize expanded Work Order controls by role: Admin+ may edit imported metadata; Supervisors edit only supervisor, technicians, status, and labor; Technicians edit notes and add items only. | Done | Added a service-layer least-privilege matrix: notes/add-material remain Technician+; operational routing/status/mode, labor, and logged-material corrections/removal require Supervisor+; Location, Service, Schedule Date, Output To, Vendor Contact, Symptom/Task, and other metadata require Admin+. The editor renders only those role-relevant controls. Verified by 415 backend tests, all 32 JavaScript syntax checks, focused role coverage, and `git diff --check`. |
| IMP-015 | 2026-08-04 | Work Orders archive | Allow Admin+ to archive a work order during any status. | Done | Removed the Review-only service restriction, added an authoritative Admin+ service gate, and exposed a confirmed Archive action on every expanded live Work Order card for Admin/Owner. Admin Review retains its receipt-aware Close action for Review rows. Verified by 427 backend tests, all 32 JavaScript syntax checks, and Python compilation. This supersedes IMP-005/IMP-007's earlier Review-only UI placement. |
| IMP-016 | 2026-08-04 | Work Orders CSV import | Do not restore archived work orders during import; report how many CSV rows are already closed and leave every closed row untouched. | Done | Import now skips archived matches before supervisor routing or metadata merge, returns a dedicated `closed` count included in `total`, and always shows that count in the completion message. Live matches retain fill-blanks-only metadata behavior; notes, assignments, lifecycle, materials, labor, billing, and existing metadata are untouched. Verified by the 428-test backend suite, including a closed-row preservation regression. |
| IMP-017 | 2026-08-04 | Work Orders CSV export | Name export files `[TIMESTAMP]_[filter1]-[filter2]`, using readable date/time. | Done | Server-generated attachment names use `MM-DD-YY_HH-MM_filters.csv` in UTC and every active operational filter value; the time separator is `-` because Windows forbids `:` in filenames. No-filter files use `_all.csv`, while For Client uses `client-<scope>`. Focused export tests cover all, multi-filter, and client filenames. |
| IMP-018 | 2026-08-04 | Work Orders supervisor pickup / CSV import | Let supervisors see unassigned work orders plus work routed to themselves so they can pick up work. If another supervisor wins a stale pickup, show `This Work Order was already assigned to [First] [Last]` and reload after the prompt closes. Re-import must assign a supervisor only while the work order remains unassigned and must preserve a manual reroute. | Done | Supervisor scope is now unassigned-or-self-routed. Edit Details sends `expected_supervisor_id`; the service locks the row, returns a named 409 on stale pickup, validates active Supervisor targets, and still returns a successful transfer even when it moves out of caller scope. Import/reference merging locks the same row and fills `supervisor_id` only when NULL, including insert-race recovery. The one-button conflict prompt reloads on dismissal, and import copy reports name matches rather than claiming every match was routed. Verified by 432 backend tests, Python compilation, changed JavaScript syntax checks, and `git diff --check`; interactive browser QA was unavailable. |
