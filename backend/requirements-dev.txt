# Dev/test dependencies. Not required to run the app.
#
# Install with:  python -m pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.0.3
# Required by `fastapi.testclient.TestClient`. Starlette 1.2.x builds its
# TestClient on httpx2 (NOT the older `httpx` package -- importing
# `fastapi.testclient` without this raises "requires the httpx2 package").
#
# No test imports TestClient today: the suite exercises routers and services
# by calling them directly, which is why it passes without this. Declaring it
# is what makes the first HTTP-level test possible without a detour.
httpx2==2.9.1
# Only needed to (re)generate the barcode image fixtures under
# tests/fixtures/. The committed PNGs mean the test suite itself does
# not import this package.
python-barcode==0.16.1

# Local-only NetFacilities Stage 1 proof of concept. No browser binary is
# downloaded during package installation, and neither dependency is included in
# the production Docker image until a later production-integration stage is
# explicitly approved.
beautifulsoup4==4.15.0
playwright==1.62.0
