# Dev/test dependencies. Not required to run the app.
#
# Install with:  python -m pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.0.3
# Only needed to (re)generate the barcode image fixtures under
# tests/fixtures/. The committed PNGs mean the test suite itself does
# not import this package.
python-barcode==0.16.1
